DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# Optional, used for daily chat rate limiting.
# If these are missing or unavailable, chat will still work and rate limiting is skipped.
KV_REST_API_URL=
KV_REST_API_TOKEN=
